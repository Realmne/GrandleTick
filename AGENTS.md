# AGENTS

## 代码编写要求

- 以后修改或新增代码时，必须为关键逻辑、非显而易见的分支、复杂数据转换和重要边界条件补充必要的中文注释。
- 注释应解释“为什么这样做”和“这段逻辑解决什么问题”，避免只重复代码表面含义。
- 简单赋值、明显的 UI 布局属性、常规函数调用等无需额外注释，避免制造噪音。

## 算法说明要求

涉及算法、复杂流程或多步骤业务逻辑时，必须在函数内部按执行阶段分块写中文序号注释。序号注释的作用是分隔过程块，而不是在函数顶部集中列一个步骤清单。

```swift
func rebuildReport() {
    // 1. 准备输入数据，过滤无效记录。
    let validLogs = logs.filter { !$0.isInvalid }

    // 2. 按业务规则计算中间结果。
    let summary = buildSummary(from: validLogs)

    // 3. 写回状态或返回最终结果。
    self.summary = summary
}
```

步骤编号必须使用 `1. 2. 3.` 这样的阿拉伯数字加英文句点格式，并放在对应代码块之前，让函数执行过程一段一段清晰可读。

## 提交要求

- 每实现完成一个功能后，必须自动创建 Git 提交并推送到远程仓库。
- 提交信息必须遵循 Conventional Commits 格式，并使用中文描述，例如：

```text
feat: 添加活动统计筛选功能
fix: 修复窗口标题解析异常
refactor: 优化数据迁移流程
```

- 提交前应检查工作区变更，避免提交与当前功能无关的文件。

## 更新后安装要求

- 每次完成代码更新并验证后，必须退出当前运行中的 GrandleTick，覆盖安装新构建到 `/Applications/GrandleTick.app`，然后启动新版应用。
- 覆盖安装前应确保构建产物来自当前已验证代码，避免把旧版本重新安装回应用目录。
